name: iris_identifier

python_env: python_env.yaml

entry_points:
  main:
    parameters:
      epochs: {type: int, default: 50}

    command: |
          python demo_mlflow.py \
            --epochs {epochs}
